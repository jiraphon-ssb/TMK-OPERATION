-- ============================================================
-- TMK Operation — ค่าธรรมเนียมแพลตฟอร์มจริงต่อช่องทาง
-- ============================================================
-- เดิมหน้า "ช่องทางการขาย" คำนวณกำไร/มาร์จิ้นด้วยค่าธรรมเนียม 5% แบบ hardcode (ไม่ใช่ค่าจริง)
-- คอลัมน์นี้ให้ตั้งค่าธรรมเนียมจริงต่อช่องทางได้ (เช่น Shopee 5–10%, CRM/ช่องทางตัวเอง = 0)
-- ค่าเริ่มต้น 0 → กำไร = รายได้ − ค่าแอด (ไม่มีตัวเลขปลอม)
-- รันใน Supabase SQL Editor
-- ============================================================

alter table public.tmk_channels
  add column if not exists platform_fee_pct numeric not null default 0;

comment on column public.tmk_channels.platform_fee_pct is
  'ค่าธรรมเนียมแพลตฟอร์ม (%) ใช้คำนวณกำไร/มาร์จิ้นจริง; 0 = ไม่มีค่าธรรมเนียม';
