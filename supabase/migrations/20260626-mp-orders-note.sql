-- ============================================================
-- TMK Operation — เพิ่มคอลัมน์ "หมายเหตุ" (note) ให้ออเดอร์นำเข้า — 2026-06-26
-- ============================================================
-- เก็บหมายเหตุจากไฟล์ Shipnity ลง tmk_mp_orders เพื่อ:
--   (1) โชว์คอลัมน์ "หมายเหตุ" ในตารางออเดอร์ + drawer รายละเอียด
--   (2) ใช้ derive ประเภทงาน DFT (หมายเหตุที่มีคำว่า DFT) ตอน import
-- ปลอดภัย/เพิ่มเฉพาะ (idempotent) — รันใน Supabase SQL Editor ได้เลย
-- ต้องรันก่อน re-import ไฟล์ Shipnity (import upsert ส่งคอลัมน์ note ไปด้วย)
-- ============================================================

alter table public.tmk_mp_orders add column if not exists note text;
